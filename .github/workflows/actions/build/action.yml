name: 'build'
description: 'Manage package build'

inputs:
  token:
    description: 'GitHub token'
    type: string
    required: true
  release_id:
    description: 'Newly release id'
    type: string
    required: true

runs:
  using: 'node20'
  main: 'src/index.js'
